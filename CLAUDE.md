Read the files in plans/ before starting!

Always use root urls (/docs/...) for links within the documentation.

Commit with the repo's default git identity — never as "Claude", and never add a Co-Authored-By or other signature trailer to the message.
