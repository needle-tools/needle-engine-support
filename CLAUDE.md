Read the files in plans/ before starting!

Always use root urls (/docs/...) for links within the documentation.

## Git Configuration

When making commits, use "Claude" as the committer name and "claude@needle.tools" as the email:
```bash
git -c user.name="Claude" -c user.email="claude@needle.tools" commit -m "..."
```